import * as React from "react";
import {
  Alert,
  Modal,
  StyleSheet,
  Text,
  ScrollView,
  View,
  TouchableOpacity,
  Image,
} from "react-native";

const Header = ({namepage}) => {
    return (


    )
};

export default Header();